--1. Check to make sure campaign table loaded
SELECT * FROM campaign;

--2. Check to make sure category table loaded
SELECT * FROM category;

--3. Check to make sure contacts table loaded
SELECT * FROM contacts;

--4. Check to make sure subcategory table loaded
SELECT * FROM subcategory;
